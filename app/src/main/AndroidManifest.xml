<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sasoftgroups.handy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginPage" />
        <activity android:name=".HomePage" />
        <activity android:name=".RegisterPage" />
        <activity android:name=".HandRequest" />
        <activity android:name=".MyProfile" />
        <activity android:name=".FriednsList" />
        <activity android:name=".GiveHand" />
        <activity android:name=".Messenger" />
        <activity android:name=".FeedbackArea" />
        <activity android:name=".RegisterPartTwo" />
        <activity android:name=".getAllUserData" />
        <activity
            android:name=".oneChat"
            android:windowSoftInputMode="adjustPan"></activity>
    </application>

</manifest>